self.addEventListener("install", (event) => {
    console.log("Service worker installing...");
    // Here, you'll later add code for actions like caching assets.
    //use the event handler here according to you want.
   });